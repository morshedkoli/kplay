<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Downloads keep running while the viewer is elsewhere in the system,
         which on modern Android means a foreground service and, from 33, the
         viewer's permission to show its notification. RECEIVE_BOOT_COMPLETED
         is what lets an interrupted download resume after the television is
         switched back on. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Not a touchscreen-required app — this is exactly what makes it
         installable/launchable on Android TV. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- largeHeap: the media buffer is Java byte arrays on the app heap, and
         a deep buffer is what keeps a high-bitrate direct-play file from
         stuttering. Without it the heap growth limit on a television box
         (often 192 MB, shared with the rest of the app) is the real ceiling
         on how much video can be held ahead of the playhead. -->
    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/Theme.KPlayTV">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.KPlayTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- LEANBACK_LAUNCHER is what makes it show up on the
                     Android TV home screen row, not just the phone launcher. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Not exported: nothing outside the app has any business starting a
             download. Media3 talks to it through explicit intents from within
             the process, which needs no export. -->
        <service
            android:name=".KDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="androidx.media3.exoplayer.downloadService.action.RESTART" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>
</manifest>
